{
  "name": "lambda-event-publisher",
  "version": "1.0.0",
  "description": "Lambda 2 – Polls SQS (ElasticMQ) for messages and publishes events to Redis Pub/Sub",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "dev": "ts-node src/app.ts"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.700.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "ts-node": "^10.9.0"
  }
}
